{% extends 'base.html.twig' %}

{% block body %}

    {% if slug is defined%}
        <p>test route with slug {{slug}} works</p>
    {% else %}
        <p>test route works</p>
    {% endif %}
    <div {{ stimulus_controller('test') }}>
        <button
            {{ stimulus_action('test', 'doSomethingAsync') }}
        >do something async</button>
    </div>
{% endblock %}